{
  "name": "serial-pilot-mcp",
  "version": "1.0.1",
  "private": true,
  "description": "Serial Pilot MCP Server — AI-powered serial debugging tools via MCP protocol",
  "main": "./dist/index.js",
  "bin": {
    "serial-pilot-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "watch": "tsc -w",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^20.0.0",
    "rimraf": "^5.0.0"
  }
}
